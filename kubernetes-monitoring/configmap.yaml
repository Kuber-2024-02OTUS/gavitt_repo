apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-conf
  namespace: homework 
data:
  default.conf: |
    server {
    listen       80;
    server_name  _;
    location /nginx_status {
        stub_status;
    }
    }
